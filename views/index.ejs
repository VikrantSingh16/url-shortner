<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="icon" type="image/x-icon" href="https://icons8.com/icon/38051/link">

    <title>Short URL</title>
</head>
<body>

<div class="container">
    <h1 class="mt-4">URL Shrinker</h1>
    <h6>Welcome <%=email%></h6>
    <form action="/user/logout" method="post">
        <button class="btn btn-danger mt-2" type="submit">Log out</button>

    </form>
    <form action="/shorturls/save<%=email%>" method="POST" class="my-4 form-inline">
        <!-- <label for="fullUrl" class="sr-only">Url</label> -->

        <input class="form-control col mr-2 " placeholder="Url" required type="url" id="fullUrl" name="fullUrl">
   <button class="btn btn-success mt-2" type="submit">Shrink</button>
    </form> 

<table class="table table-striped table-responsive">
    <thead>
        <tr>
        <th>Full URL</th>
        <th>Short URL</th>
        <th>Clicks</th>
        <th>Delete</th>
    </tr>
    </thead>
    <tbody>
        <% shortUrls.forEach(shortUrl=> {  %>
            <tr>
                <td><a href="<%= shortUrl.URL %>"><%= shortUrl.URL %></a></td>
                <td><a href="<%=shortUrl.URL%>"><%= shortUrl.shortURL %></a></td>
                <td><%= shortUrl.count %></td>
                <td>
                    <form action="/shorturls/delete/<%= shortUrl.shortURL %>" method="POST">
                        <button class="btn btn-danger mt-2" type="submit">Delete</button>

                    </form>
                </td>
            </tr>
            
      <%  })%>
       
    </tbody>
</table>
</div>
</body>
</html>